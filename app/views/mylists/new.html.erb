<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Mylist
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="#">top5</a>
              <a class="dropdown-item" href="#">new lease</a>
              <a class="dropdown-item" href="#">top rating</a>
              <% @current_user.mylists.each do |m| %>
              <a class="dropdown-item" href="#"> <%= m.title %></a>
                <%end  %>
            </div>
          </li>
       </ul>
    </div>
</nav>


<h1>My new list</h1>

<%= form_for @mylist do |f| %>
<%= f.label :title %>
<%=  f.text_field :title, :placeholder =>'make a name '  %>
<%= f.submit  %>
<% end %>
